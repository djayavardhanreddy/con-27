{
  "name": "con-27-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace of con-27 Global Nursing Conference 2027",
  "scripts": {
    "dev:backend": "cd backend && uvicorn main:app --reload --port 5000",
    "dev:frontend": "pnpm --filter con-27-frontend dev",
    "dev": "pnpm run dev:frontend",
    "build:backend": "echo \"Python backend does not require compilation\"",
    "build:frontend": "pnpm --filter con-27-frontend build",
    "build": "pnpm run build:frontend"
  },
  "devDependencies": {}
}
